const http = require('http')

const showPokemon = (req, res) => {
    const options = {
        hostname: 'api.example.com',
        path: '/data',
        method: 'GET'
      };
    // petición a la API

}